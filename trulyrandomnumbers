
#include <iostream>
#include <cstdlib>
#include <ctime>
using namespace std; 
int main()
{
    srand(time(0));
    //the remander operator is used to specify to generate a number 0-6 
    for(int x = 1; x<25; x++){
        cout << 1+ (rand()%6) << endl;
    }
    return 0;
}

